import React from 'react'
import {Title} from 'react-native-paper'

const Empty = (props) => {
    return (
        <Title
        style={{color:'gray', alignSelf:'center', paddingTop:'50%'}}
        >Lista de Eventos vacia</Title>
    )
}

export default Empty
